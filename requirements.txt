# Core numerics — used everywhere
numpy>=1.26
scipy>=1.12

# Raster I/O — reads and writes GeoTIFFs
rasterio>=1.3

# UI
streamlit>=1.30

# Visualisation / image export
matplotlib>=3.8
pillow>=10.0

# Optional: only needed to run scripts/fetch_from_gee.py.
# Safe to skip if you already have GeoTIFFs to upload.
earthengine-api>=0.1.400
